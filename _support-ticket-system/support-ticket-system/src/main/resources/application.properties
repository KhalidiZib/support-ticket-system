# ========== ACTIVE PROFILE ==========
spring.profiles.active=dev

# ========== SERVER CONFIGURATION ==========
server.port=8081
# server.servlet.context-path=/support-api
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
server.compression.enabled=true

# ========== DATABASE CONFIGURATION ==========
spring.datasource.url=jdbc:postgresql://localhost:5433/support_ticket_db
spring.datasource.username=postgres
spring.datasource.password=K100921Z
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Optimization
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# ========== JPA & HIBERNATE CONFIGURATION ==========
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ========== JWT SECURITY CONFIGURATION ==========
# IMPORTANT: Change this in production using environment variable
app.jwt.secret=${JWT_SECRET:mySecretKeyForJWTGenerationInSupportTicketSystem2024RwandaSupportTicketApp2024SecureRandomString}
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# ========== EMAIL CONFIGURATION ==========
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kzibika@gmail.com
# IMPORTANT: Replace with Gmail App Password (not your regular password)
# Go to: Google Account > Security > 2-Step Verification > App Passwords
spring.mail.password=vdbw esru hzkh adme
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.test-connection=false

# ========== FILE UPLOAD CONFIGURATION ==========
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========== CORS CONFIGURATION ==========
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
app.cors.allow-credentials=true
app.cors.max-age=3600

# ========== LOGGING CONFIGURATION ==========
logging.level.root=INFO
logging.level.com.ticketsystem=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.security=DEBUG
logging.level.com.ticketsystem.security=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.file.name=server.log

# ========== ACTUATOR CONFIGURATION ==========
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized
management.health.db.enabled=true
management.health.mail.enabled=false

# ========== PAGINATION CONFIGURATION ==========
app.pagination.default-page-size=10
app.pagination.max-page-size=50

# ========== TWO-FACTOR AUTHENTICATION ==========
app.2fa.enabled=true
app.2fa.issuer=SupportTicketSystem